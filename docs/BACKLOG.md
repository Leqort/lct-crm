# Что осталось сделать — передача второму разработчику

Статус на 15.09.2026. Канонический список задач; ID требований — из `docs/TZ-IT-School-RTK.md`.

---

## Базовая линия: что уже работает

Сделан **SPEC-01** (`first-move-spec.md`) — ядро данных — плюс подключён Keycloak.

| Блок | Состояние |
|---|---|
| Справочники: вузы, вендоры, ИТ-направления, ИТ-продукты, контакты вузов | CRUD + мягкое удаление + поиск, сортировка, пагинация |
| `interactions` — карточка «вуз + направление + продукт» | CRUD, бизнес-ключ, upsert |
| Импорт xls/xlsx (`SRV-01`, все 10 полей `FLD-01…10`) | Загрузка → маппинг → предпросмотр → запись → отчёт об ошибках |
| Аудит (`audit_log`) | Автоматический, append-only, ПДн маскируются |
| Роли и видимость (`FR-12`) | `user` / `manager` / `admin`, фильтр на уровне репозитория |
| Keycloak (`FR-11`) | Realm `crm`, роли, вход через Swagger, JWT проверяется по JWKS |
| Коды ошибок (`NFR-03`) | Единый конверт, 9 кодов |
| Swagger (`SOL-02`) | Все методы описаны, перечень библиотек в README |
| Docker (`SOL-04`) | `docker compose up` — postgres + keycloak + api, миграции в entrypoint |

Проверено: 127 тестов, `ruff check`, `ruff format --check`, `mypy app` — чисто.
Голова миграций: `0003_audit_append_only`. Запуск: `docker compose up`.

---

## Как подключаться к существующему коду

Три вещи, которые экономят день на старте.

**Аудит достаётся бесплатно.** Унаследуй модель от `DomainBase` (`app/models/base.py`) — получишь UUID-PK, `created_at/updated_at/created_by/updated_by`, мягкое удаление и автоматическую запись в `audit_log` на каждый insert/update. Ничего вызывать руками не нужно. Для не-ORM событий (переход по этапу, выгрузка файла) — `app/services/audit.py::log_event`.

**Видимость достаётся бесплатно.** Если новая таблица привязана к вузу, задай в репозитории `university_scope_column = "university_id"` — и роль `user` автоматически будет видеть только свои записи. Фильтр живёт в `BaseRepository`, роутер про него не знает.

**Не затирай заполненное пустым.** Правило «пустая ячейка = нет данных» действует во всём импорте — см. `InteractionService.upsert`. Тот же принцип нужен переходам и интеграциям.

Полезные точки входа: `app/services/catalogs.py` (find-or-create для всех справочников — вызывать из интеграций, а не дублировать), `app/services/text.py` (нормализация названий), `app/core/errors.py` (коды ошибок).

---

## Трек A. Workflow-движок — **брать первым**

Это следующий смысловой блок и половина критериев финальной экспертизы (`EVF-03`). Заглушки под него уже стоят: `interactions.workflow_version_id` и `interactions.current_stage_id` — nullable UUID **без** внешнего ключа, добавить FK отдельной миграцией.

| ID | Задача | Требование | Критерий приёмки |
|---|---|---|---|
| **A1** | Схема workflow: шаблон → версия → этапы → допустимые переходы; история переходов по карточке; комментарии к переходу | `FR-03`, `FR-07` | Миграция поверх `0003`; FK с `interactions` на версию и текущий этап |
| **A2** | Предзаполнить базовый workflow из 14 шагов и оставить его редактируемым | `WF-BASE`, `FR-07` | Сид создаёт `WF-01…WF-14`; шаги можно переименовать и переупорядочить |
| **A3** | API перехода по этапам с проверкой допустимости и обязательным комментарием | `FR-03` | Недопустимый переход → `422` с кодом; комментарий попадает в историю и в аудит |
| **A4** | Переименование этапов | `FR-03` | Переименование не ломает карточки, уже стоящие на этом этапе |
| **A5** | Вложения к этапам: png, jpeg, pdf, zip, gzip, rar, doc, docx, xls, xlsx | `FR-04` | Все 10 расширений принимаются, тип проверяется по сигнатуре файла, а не по имени; файл привязан к конкретному этапу; загрузка и скачивание пишутся в аудит |
| **A6** | Версионирование: правка workflow не должна переписывать путь карточек, уже находящихся в работе | `FR-07`, `FR-10` | Карточка помнит версию, по которой стартовала |
| **A7** | Отдать фронтенду граф этапов и переходов для визуализации пути | `FR-03` | Один запрос отдаёт всё, что нужно для отрисовки схемы |

**Важно про A5:** вложения — это новое место хранения потенциальных ПДн. Решите с заказчиком, где они лежат (том, S3/MinIO) и как долго, до того как писать код.

---

## Трек B. Отчёты и статистика — **брать параллельно**

`EVF-03` проверяет отчёты напрямую, а `EVP-03` — ещё на предварительной экспертизе.

| ID | Задача | Требование | Критерий приёмки |
|---|---|---|---|
| **B1** | Фильтр по периоду в `/interactions` | `FR-01` | Сейчас есть фильтры по вузу, направлению, продукту, ответственному — **периода нет**. Индексы по `license_signed_at` и `license_expires_at` уже заведены. Маленькая задача, годится как разминка |
| **B2** | Конструктор отчётов: выбор колонок из пяти (вуз, ИТ-направление, ИТ-продукт, статус работы с вузом, ответственный), выгрузка в xls, xlsx, pdf | `FR-05`, `FR-09`, `UC-U-03` | Ответ — файл, а не только экранная таблица. Образец генерации xlsx: `app/imports/report.py` |
| **B3** | Диаграммы и графики с экспортом в png и pdf | `FR-02` | Достоверность диаграмм оценивается отдельным критерием — цифры на графике должны сходиться с выгрузкой |
| **B4** | Асинхронная генерация отчётов | `NFR-07` | Не менее 10 параллельных отчётов разной сложности без деградации |
| **B5** | Формирование результирующего JSON-файла | `SOL-04` | — |

Колонка «статус работы с вузом» берётся из трека A — до его готовности отчёт собирается по `transfer_status` из импорта.

---

## Трек C. Интеграции — частично заблокировано заказчиком

| ID | Задача | Требование | Критерий приёмки |
|---|---|---|---|
| **C1** | Приём данных из LMS по API в формате JSON | `FR-06`, `UC-U-02` | Данные попадают в справочники **через сервисный слой** (`find_or_create`), а не мимо него |
| **C2** | Приём данных с веб-сайта на CMS Laravel в формате JSON | `FR-06`, `UC-U-02` | То же |
| **C3** | Полученные данные добавляются в существующий или новый workflow | `FR-06` | Зависит от трека A |

**Заблокировано:** `OPEN-01` (контракт API LMS и сайта) и `OPEN-04` (перечень утверждённых полей для JSON) — по ТЗ предоставляются заказчиком. До этого можно сделать каркас: точку приёма, валидацию, идемпотентность по внешнему ключу.

---

## Трек D. Фронтенд — **брать параллельно**

Без него нет `DLV-03` (работающий прототип с веб-интерфейсом) и половины критериев `EVF-04`. Стек по ТЗ — React.

| ID | Задача | Требование |
|---|---|---|
| **D1** | Каркас SPA + вход через Keycloak (PKCE, обновление токена) | `FR-13`, `FR-11` |
| **D2** | Справочники и мастер импорта в 5 шагов — API готов полностью | `SRV-01` |
| **D3** | Визуализация пути взаимодействия, переходы, комментарии, вложения | `FR-03`, `FR-04` |
| **D4** | Фильтры, отчёты, диаграммы | `FR-01`, `FR-02`, `FR-05` |
| **D5** | Кэш действий пользователя | `FR-14` |
| **D6** | Десктоп + мобильная версия, без перезагрузки страницы при изменениях | `UX-04`, `NFR-02` |

---

## Трек E. Права и администрирование

| ID | Задача | Требование |
|---|---|---|
| **E1** | Подтвердить маппинг ролей ТЗ на системные: сейчас «Руководитель» = `manager`, «Администратор» = `admin`. В ТЗ у Руководителя явно названо управление ответственными за вузы — это уже реализовано для `manager` | `FR-12` |
| **E2** | Админ-настройки ограничений видимости данных сверх закрепления за вузом | `UC-A-01` |
| **E3** | Заведение ~20 КАМов в Keycloak и синхронизация с локальной проекцией | `FR-11`, `FR-12` |

---

## Трек F. Нефункциональные требования и эксплуатация

| ID | Задача | Требование |
|---|---|---|
| **F1** | Нагрузочные тесты: 50 параллельных пользователей, 10 параллельных отчётов, отклик ≤ 1 с на ключевых операциях | `NFR-01`, `NFR-06`, `NFR-07` |
| **F2** | CI: прогон `ruff`, `mypy`, `pytest` на каждый пуш — сейчас пайплайна нет | `EVP-02` |
| **F3** | Продовый контур Keycloak: реальный realm, секреты вне репозитория, HTTPS. Локальный `docker/keycloak/realm-export.json` — только для разработки | `FR-11` |
| **F4** | Меры ИБ по 152-ФЗ и приказу ФСТЭК № 117: заполнить матрицу мер, зафиксировать, что закрыто аудитом и мягким удалением, а что требует инфраструктуры | `FR-08`, `SOL-10` |
| **F5** | Партиционирование `audit_log` по месяцам и политика ретенции `import_rows` (там лежат ФИО контактов вузов) | — (техдолг SPEC-01) |
| **F6** | Оптимизация dry-run импорта: сейчас на каждую строку идут отдельные запросы за вузом и менеджером. На файле в тысячи строк это заметно — нужен кэш в рамках задачи импорта | `NFR-01` |

---

## Трек G. Документация и сдача

| ID | Задача | Требование |
|---|---|---|
| **G1** | Руководство пользователя и руководство системного администратора со скриншотами интерфейса, **встроенные в платформу** | `NFR-05` |
| **G2** | Описание функциональной и компонентной архитектуры **в Archi** | `SOL-07` |
| **G3** | Презентация (pptx или pdf) и ссылка на работающий прототип | `PRES-01`, `DLV-02`, `DLV-03` |
| **G4** | Сопроводительная документация: методы обработки данных, условия и ограничения решения, инструкции по сборке и установке | `SOL-06`, `SOL-07`, `DLV-04` |

---

## Решения, которые принимает заказчик, а не разработчик

Пока не закрыты — держать на виду.

| Вопрос | Почему важно |
|---|---|
| Хеширование ПДн в аудите без соли | Несолёный sha256 телефона или email теоретически подбирается по словарю. Альтернатива — HMAC с ключом, но тогда хеши нельзя сравнивать между записями |
| Состав полей, отнесённых к ПДн | Сейчас `full_name`, `email`, `phone`. Если заказчик добавит, например, должность — это одна строка в модели |
| Срок хранения `import_rows` | Там лежат сырые строки Excel с ФИО контактов вузов. Автоочистки сознательно нет |
| Хранение вложений к этапам (трек A5) | Новое место для ПДн — решить до реализации |
| `OPEN-01`, `OPEN-04` | Контракты API LMS и сайта, перечень полей JSON |

---

## Рекомендуемый порядок

1. **B1** — фильтр по периоду. Полчаса работы, знакомство с кодовой базой.
2. **A1–A3** — схема workflow, базовые 14 шагов, переходы. Разблокирует и фронтенд, и колонку «статус» в отчётах.
3. Дальше **A** и **B** идут параллельно с **D**; **C** ждёт контрактов от заказчика.
